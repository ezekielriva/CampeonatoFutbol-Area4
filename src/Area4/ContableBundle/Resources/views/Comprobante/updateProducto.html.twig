<h1>Items edit: {{entity.id}}</h1>

<form action="{{ path('items_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('items') }}">
            Back to the list
        </a>
    </li>

</ul>
