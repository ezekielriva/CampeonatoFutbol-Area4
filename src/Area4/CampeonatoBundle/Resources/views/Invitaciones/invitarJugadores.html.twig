<h1>invitacion creation</h1>

{% form_theme form '::form_register.html.twig' %}
<form action="{{ path('invitaciones_jugador_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('invitaciones') }}">
            Back to the list
        </a>
    </li>
</ul>