{% form_theme form '::form_register.html.twig' %}
<form action="{{ path('jugador_create') }}" method="post" {{ form_enctype(form) }} id="formJugadorNew">
 	<fieldset>
		<legend>Datos del Jugador</legend>
    	{{ form_widget(form) }}
    </fieldset><br/>
</form>

{#<ul class="record_actions">
    <li>
        <a href="{{ path('jugador') }}">
            Back to the list
        </a>
    </li>
</ul>#}

