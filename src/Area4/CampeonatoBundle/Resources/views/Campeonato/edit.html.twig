<form id="form" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" onclick="submitForm('#dialog','#form','{{ path('campeonato_update', { 'id': entity.id }) }}')">
            Editar
        </button>
    </p>
</form>
<form action="{{ path('campeonato_delete', { 'id': entity.id }) }}" method="post">
    {{ form_widget(delete_form) }}
    <button type="submit">Delete</button>
</form>
<script type="text/javascript">
cargarBotonesDialog('#dialog');
</script>