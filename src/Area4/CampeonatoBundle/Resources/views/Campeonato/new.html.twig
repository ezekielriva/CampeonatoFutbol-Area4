{% form_theme form '::form_register.html.twig' %}

<form action="{{ path('campeonato_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Crear</button>
    </p>
</form>
<script type="text/javascript">
            $( "button:submit" ).button();
        </script>