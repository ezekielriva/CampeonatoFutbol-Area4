<h1>Partido creation</h1>

<form action="{{ path('partido_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('partido') }}">
            Back to the list
        </a>
    </li>
</ul>
